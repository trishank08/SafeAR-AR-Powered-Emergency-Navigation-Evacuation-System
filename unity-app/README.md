# SafeAR Unity client
This is the AR client integration layer. Open with Unity 6.3 LTS, install AR Foundation 6.x and Google ARCore XR Plug-in, then create an AR Session + AR Session Origin/AR Camera. The scripts intentionally do not claim indoor positioning automatically: route waypoints must be configured and mapped into the controlled building coordinate system.
