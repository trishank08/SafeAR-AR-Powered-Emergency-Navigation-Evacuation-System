# Deployment
Use a managed MongoDB, deploy Node and Flask separately, restrict CORS to the deployed frontend, terminate TLS at the edge, store secrets in the platform secret manager, and expose only the public API ports. Unity builds should target supported Android devices and use HTTPS for production API traffic.
