# Unity / ARCore
Use Unity 6.3 LTS with AR Foundation and Google ARCore XR Plug-in. The server returns waypoint IDs; the Unity client maps them to configured local anchors/coordinates. AR support must be checked at runtime. If unsupported or tracking is unavailable, show map/distance navigation instead of claiming AR is active.
